node_modules/
dist/
*.tsbuildinfo

.idea/
.vscode/

# Local env overrides. Only the template is committed.
.env
.env.*
!.env.example

# Vercel CLI workspace (holds the linked project id and pulled env values).
.vercel
