// Superseded by TeachersPage (the super admin console's Teachers tab). Kept as a
// re-export so any lingering import resolves; safe to delete once none remain.
export { default } from "./TeachersPage";
